const errorHandel = require('./error-handle');
const requestParamsExtract = require('./request-params-extract');
const responseMapping = require('./response-mapping');

module.exports = {
  errorHandel,
  requestParamsExtract,
  responseMapping,
};
